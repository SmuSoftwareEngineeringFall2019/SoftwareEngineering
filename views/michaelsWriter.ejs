<!DOCTYPE html>
<html>

<head>
  <title>Writer | Start Your Story</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta charset="utf-8">
  <link rel="stylesheet" href="/css/quickWriter.css">
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  <script src="/scripts/quick.js"></script>
</head>

<body onload="startUp()" onunload="checkSave()">
  <header>
    <!----Mikes Logo DO WE NEED A LOGO??-----
    <img id="logo" alt="logo"-->
    <%if (!blog) {%>
    <input id="title" onclick="checkField(0)" placeholder="Enter Title">
    <%} else {%>
    <input id="title" onclick="checkField(0)" value="<%=blog.title%>">
    <%}%>

    <!-- if blog is not null edit() otherwise publish -->

    <%if (!blog) {%>
    <button onclick="publish()">Publish</button>
    <span id="username" hidden><%= JSON.stringify(userName);%></span>
    <%} else {%>
    <button onclick="edit()">Submit Changes</button>
    <%}%>
  </header>
  <!--here i changed the class from your textArea to use-keyboard-input
  which is in the quickWriter.js file-->
  <%if (!blog) {%>
  <textarea autofocus placeholder="Share Your Story..." class="use-keyboard-input" onclick="checkField(1)" id="userText"></textarea>
  <%} else {%>
  <textarea autofocus class="use-keyboard-input" onclick="checkField(1)" 
  id="userText"><%=blog.body%></textarea>
  <span id="blogId" hidden>
  <%= JSON.stringify(blog._id);%>
  </span>
  <span id="userId" hidden>
  <%= JSON.stringify(blog.author.username);%>
  </span>
  <%}%>

</body>

</html>
